# ─── Credora FE — environment (NOT YET WIRED) ───────────────────────────
# These are placeholders consumed only by lib/contract.ts when DATA_SOURCE is
# flipped to "onchain". The app runs fully on mock data without any of them.

# Mantle Sepolia testnet RPC
NEXT_PUBLIC_MANTLE_RPC_URL=

# Deployed contract addresses (from the smart-contract repo)
NEXT_PUBLIC_AGENT_REGISTRY=
NEXT_PUBLIC_DECISION_LOGGER=
NEXT_PUBLIC_REPUTATION_SCORE=
